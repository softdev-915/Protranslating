import SchedulerSection from './scheduler-section.vue';
import SectionContainer from '../../section-container/section-container.vue';

export default {
  components: {
    SchedulerSection,
    SectionContainer,
  },
};
