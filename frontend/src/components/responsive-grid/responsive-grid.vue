<template>
  <div>
    <grid-table
      :title="title"
      ref="gridTable"
      :list-data="listData"
      :columns="configuredColumns"
      :grid-configs="gridConfigs"
      :key-for-item="keyForItem"
      :page="page"
      :sort="sort"
      :limit="limit"
      :q="q"
      :table-class="tableClass"
      :filter="filter"
      :use-header-filter="useHeaderFilter"
      :use-reset-filter-link="useResetFilterLink"
      :save-grid-config-available="saveGridConfigAvailable"
      :can-create="canCreate"
      :can-setup="canSetup"
      :can-export="canExport"
      :can-sort="canSort"
      :can-sort-by-select="canSortBySelect"
      :can-toggle="canToggle"
      :loading="loading"
      :exportingData="exportingData"
      :components="components"
      :rowSelection="rowSelection"
      :rowSelectionTitle="rowSelectionTitle"
      :rowHrefBuilder="rowHrefBuilder"
      :selectedRowCheckProperty="selectedRowCheckProperty"
      :selectedRows="selectedRows"
      :activeRows="activeRows"
      :grid-name="gridName"
      :grid-config-applied="gridConfigApplied"
      :is-pagination-enabled="isPaginationEnabled"
      :is-scroll-pagination-enabled="isScrollPaginationEnabled"
      :has-show-all-records-link="hasShowAllRecordsLink"
      :has-import-link="hasImportLink"
      :row-selection-disabled="rowSelectionDisabled"
      :css-row-class="cssRowClass"
      :custom-pagination-entries="customPaginationEntries"
      :show-select-all-button="showSelectAllButton"
      @all-rows-selected="onSelectAll"
      @row-selected="onRowSelected"
      @grid-row-toggle="onGridRowToggle($event)"
      @grid-scroll="onGridScroll"
      @grid-reset-query="onGridResetQuery($event)"
      @grid-entry-change="onEntryChange($event)"
      @grid-sort="onSort($event)"
      @grid-filter="onFilter($event)"
      @grid-page-change="onPageChange($event)"
      @grid-edit="onEdit($event)"
      @item-action="itemAction($event)"
      @grid-search="onSearch($event)"
      @grid-data-export="onGridDataExport($event)"
      @grid-data-import="onGridDataImport($event)"
      @grid-show-all-records="onGridShowAllRecords()"
      @grid-column-change="onGridColumnChange($event)"
      @grid-column-move="onGridColumnMove($event)"
      @grid-config-save="onGridConfigSave($event)"
      @grid-config-new="onGridConfigNew($event)"
      @grid-config-selected="onGridConfigSelected($event)"
      @grid-config-delete="onGridConfigDelete($event)"
      @grid-create-new="onGridCreateNew($event)"
      @grid-setup-action="onGridSetupAction($event)"
      @grid-select-all-columns="onGridSelectAllColumns($event)"/>
    <slot name="edit-modal"></slot>
  </div>
</template>

<script src="./responsive-grid.js"></script>

<style scoped lang="scss" src="./responsive-grid.scss"></style>
